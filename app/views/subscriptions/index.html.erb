<%= render 'shared/chartindex', datasindex: @dataindex %>
<%= render 'shared/cardmonthly', user: @user %>

<% @subscriptions.each do |subscription|%>
  <%= link_to subscription_path(subscription) do %>
    <%= render 'shared/card', subscription: subscription %>
  <% end %>
<% end %>
